manifestVersion: v1

deployment:
  ggn:
    service: webhooks
    pod: aci.blbl.cr/pod-webhooks

versionControlSystem:
  branch: master
  stash:
    repository: repository
    project: project

repositoryManager:
  nexus:
    artifact: pod-webhooks

hooks:
  slack:
    channel:   channel
