manifestVersion: v1

deployment:
  ggn:
    service: webhooks
    pod: pod-webhooks

versionControlSystem:
  stash:
    branch: master
    repository: repository
    project: project

repositoryManager:
  nexus:
    artifact: pod-webhooks

hooks:
  slack:
    channel:   channel
  newRelic:
    applicationId: "123"
  execCommand:
    onPredeploy:
      - { command: "ls", args: ["-lah"] }
    onPostDeploy:
      - { command: "cd /tmp"}
    stopOnError: true
