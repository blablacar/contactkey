manifestVersion: v1

deployment:
  ggn:
    service: webhooks
    pod: pod-webhooks

versionControlSystem:
  branch: master
  stash:
    repository: repository
    project: project

repositoryManager:
  nexus:
    artifact: pod-webhooks

hooks:
  slack:
    channel:   channel
  execCommand:
    list:
      - "cd /tmp"
      - "ls"
    stopOnError: true