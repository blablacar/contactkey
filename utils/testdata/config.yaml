workPath: /tmp

globalEnvironments:
  - preprod
  - prod

deployers:
  ggn:
    workPath: /tmp
    environments:
      preprod: pp
      prod: da-prod

versionControlSystem:
  stash:
    user:       user
    password:   password
    url:        url